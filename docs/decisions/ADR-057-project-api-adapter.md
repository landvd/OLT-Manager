# ADR-057：项目管理 API 适配器

## 状态

已接受，2026-08-20。

## 决策

新增 `src/project-api.mjs`，集中项目列表、新建/编辑/删除、项目 ONU 列表、备注更新和本地关联删除的 HTTP 路由合同。页面仍负责认证客户端注入、确认弹窗、加载动画、响应式状态和提示文案。

适配器统一 URL 编码、请求体清洗和错误读取，不把表单中的未知字段或密码样字段发送到项目 API；不新增任何 OLT 设备写操作。

## 验证

专项测试覆盖列表/保存 payload 脱敏、路径编码和稳定错误语义；真实页面 UI 仍由发行/现场门禁验证。
