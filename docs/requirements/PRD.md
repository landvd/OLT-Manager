# OLT Manager PRD

## 背景

维护 GPON OLT 时，经常需要在多个信息源之间切换：OLT 状态、PON 台账、ONU 序列号、光功率、距离、外层 VLAN、未注册 ONU 和现场地址。OLT Manager 的目标是把这些只读信息汇总到一个本地 Web 工具中，降低查询和核对成本。

## 目标用户

- 宽带/园区网络维护人员
- 需要核对 ONU、PON、VLAN、地址台账的一线工程师
- 需要做只读排障和现场信息汇总的管理员

## MVP 目标

- 查看 OLT 基本状态和 SNMP 可达性。
- 查询 ONU 列表并按地址、序列号、板卡、PON、状态、光功率过滤，并展示完整 `槽/板卡/PON/ID` 坐标。
- 查看 ONU 详情，包括 PON、地址、序列号、光功率、距离、VLAN 和配置片段。
- 查询未注册 ONU/ONT。
- 未注册 ONU/ONT 列表展示槽、板卡、PON、地址、序列号、发现时间和状态，地址由本地 PON 台账按 `槽/板卡/PON` 匹配。
- 从未注册 ONU/ONT 生成可复制的配置方案预览，包括 ZTE 自营上网、内部网络、自定义 VLAN、MDU+OTT，以及 Huawei 自营上网、内部网络、自定义 VLAN 模板。
- 配置方案弹窗支持复制命令和打开桌面版内置 Telnet 终端自动登录，方便人工粘贴确认。
- 首页作为运维概览，展示当前 OLT、SNMP 状态、未注册 ONU、异常 ONU、台账健康和快捷入口；桌面版快捷入口支持打开当前 OLT 的内置 Telnet 终端。
- 管理本地 OLT 和 PON 台账，PON 台账支持页面编辑、搜索、完整列表展示、Excel 导入导出和外层 VLAN 刷新；ONU 数据管理默认只显示当前 OLT 台账，输入关键字后全局搜索，外层 VLAN 刷新只针对当前选择 OLT。
- 管理本地项目，项目包含项目名称、项目 VLAN、项目地址、联系人姓名、联系人电话和联系人备注；项目名称全局唯一，项目 VLAN 为 `1-4094` 的单个 VLAN。
- 从 ONU 数据查询中把已注册 ONU 加入项目，项目详情支持查看和移除项目 ONU；移除只删除本地项目关联，不删除本地 ONU 台账，不删除 OLT 实机 ONU，不执行任何设备命令。
- ONU 安装查询的配置方案弹窗支持项目模板，例如 `项目:厚街中学(VLAN号:123)`；项目模板复用对应厂商内部网络命令结构，把 VLAN 替换为项目 VLAN。
- 记录 SNMP 测试历史和管理操作日志。
- 保持设备数据读取只读，配置命令必须人工粘贴和确认。
- 支持 Apple Silicon macOS 和 Windows 7 x64 桌面发行包，桌面版仍复用本地只读 Web 服务。
- Windows 7 x64 桌面发行包内置 SQLite CLI，安装后启动不要求用户手动配置 SQLite PATH。

## 项目管理 MVP

### 问题

现场维护时，一个项目的 ONU 可能分布在多台 OLT、多个 PON 口和多个地址。维护人员需要把这些 ONU 按项目归集，并在后续 ONU 安装时复用项目 VLAN 生成内部网络配置方案。如果只依赖 PON 台账、ONU 查询列表和手工 VLAN 输入，容易出现重复查找、重复录入和 VLAN 填错的问题。

### 方案

新增本地项目管理能力。项目只存在本地 SQLite 中，不绑定某一台 OLT，也不对应 OLT 实机对象。项目用于管理项目资料、项目 VLAN 和项目 ONU 关联，并在未注册 ONU/ONT 生成配置方案时提供项目模板。

项目字段：

- `项目名称`：必填，全局唯一。
- `项目 VLAN`：必填，单个 VLAN，范围 `1-4094`。
- `项目地址`：选填，因为同一项目的 ONU 可能分布在不同地址。
- `联系人姓名`：选填。
- `联系人电话`：选填。
- `联系人备注`：选填。

项目管理页面作为一级菜单展示，支持按项目名称、地址、联系人和 VLAN 搜索。项目资料可以编辑；修改项目 VLAN 只影响以后生成的新配置方案，不回写历史方案，也不触发任何 OLT 设备操作。

项目删除只删除本地项目和项目-ONU 关联，不删除本地 ONU 台账，不删除 OLT 实机 ONU，不执行 SNMP 写入、Telnet 配置命令、ONU 删除、重启或保存配置。

### 项目 ONU 管理

维护人员可以从 `ONU 数据查询` 把已注册 ONU 加入项目。ONU 在项目关联中的唯一身份为 `oltId + chassis + board + pon + onuId`。同一个 ONU 只能属于一个项目；如果该 ONU 已经属于其他项目，系统提示管理员先从原项目移除后再添加，不自动转移。

`ONU 数据查询` 表格展示 `所属项目` 列，方便维护人员识别已归集的 ONU。添加 ONU 到项目时先快速添加；项目详情页支持编辑项目 ONU 备注。

项目详情页展示项目 ONU 列表，字段包括：

- OLT。
- `槽/板卡/PON/ID`。
- SN。
- 在线状态。
- 光功率。
- 距离。
- 地址。
- VLAN。
- 添加时间。
- 项目 ONU 备注。

项目详情打开时尽量按现有 ONU 查询逻辑刷新状态；如果设备离线或读取失败，保留加入项目时保存的 SN、地址等快照，并提示当前状态读取失败。

### 项目模板

`ONU 安装查询` 的配置方案弹窗中，模板列表直接展示项目模板，命名格式为 `项目:厚街中学(VLAN号:123)`。用户手动选择项目模板后，系统复用对应厂商的内部网络命令结构，把 VLAN 替换为项目 VLAN，不再要求用户手动输入该 VLAN。

项目模板必须支持已支持内部网络模板的 ZTE 和 Huawei 设备。当前适用边界为：

- `zte-c300`：支持项目模板，复用 ZTE 内部网络结构。
- `huawei-ma5800`：支持项目模板，复用 Huawei 内部网络结构。
- `zte-c600` 等现有不支持配置方案生成的设备继续阻止生成，不因为项目模板放开。

项目模板仍只生成命令预览。系统不自动粘贴、不自动执行、不保存配置；桌面版内置 Telnet 终端仍只辅助人工登录和粘贴确认。

### 实施与测试边界

实现时优先复用现有本地 SQLite、HTTP API、ONU 数据查询和配置方案渲染边界。项目能力需要新增本地项目和项目-ONU 关联数据结构，并在 API 中提供项目列表、新建、编辑、删除、添加 ONU、移除 ONU 和编辑项目 ONU 备注能力。项目数据只写本地 SQLite，不连接 OLT，不触发 SNMP 写操作或 Telnet 配置命令。

实现拆分按 GitHub issues 推进：#16 只交付项目列表、新建、编辑、搜索、删除和本地项目-ONU 关联清理边界；从 ONU 数据查询添加 ONU 到项目由 #17 交付；项目详情中的项目 ONU 移除和备注编辑由 #18 交付；ONU 安装查询项目模板由 #19 交付。

测试应优先覆盖外部可见行为，而不是内部实现细节：

- 项目名称全局唯一，项目 VLAN 只能保存 `1-4094`。
- 删除项目只删除本地项目和项目-ONU 关联。
- 同一个 ONU 不能加入多个项目，重复添加时返回明确提示。
- ONU 数据查询返回所属项目信息。
- 项目模板使用项目 VLAN 生成 ZTE 和 Huawei 内部网络结构命令预览。
- 未支持配置方案的设备继续阻止项目模板生成。
- 项目模板接口只返回命令预览，不执行、不粘贴、不保存配置。

配置方案相关测试应参考现有 Node test 中对 ZTE/Huawei 模板、VLAN 变量和阻止生成逻辑的样例；项目管理 API 测试应从 HTTP API 或数据库公开行为验证，不依赖页面内部状态。

### 用户故事

1. 作为维护人员，我想新建项目并录入项目 VLAN，以便后续为该项目 ONU 生成一致的内部网络方案。
2. 作为维护人员，我想项目名称全局唯一，以便避免把 ONU 加入错误的同名项目。
3. 作为维护人员，我想项目地址可以选填，以便同一项目的 ONU 分布在多个地址时不用填误导性地址。
4. 作为维护人员，我想记录联系人姓名、电话和备注，以便现场沟通时能快速找到责任人。
5. 作为维护人员，我想按项目名称、地址、联系人和 VLAN 搜索项目，以便快速找到项目。
6. 作为维护人员，我想编辑项目资料，以便项目 VLAN 或联系人变更后可以更新本地资料。
7. 作为维护人员，我想删除项目时只删除本地项目和关联，以便整理项目列表时不会影响 OLT 实机。
8. 作为维护人员，我想从 ONU 数据查询中把 ONU 加入项目，以便不用重复录入 ONU 坐标和 SN。
9. 作为维护人员，我想看到 ONU 数据查询中的所属项目列，以便知道某个 ONU 是否已经归集。
10. 作为维护人员，我想一个 ONU 只能属于一个项目，以便避免同一 ONU 被多个项目重复管理。
11. 作为维护人员，我想重复添加已归属 ONU 时看到明确提示，以便手工决定是否先从原项目移除。
12. 作为维护人员，我想在项目详情中查看项目 ONU 的 OLT、坐标、SN、状态、光功率、距离、地址和 VLAN，以便集中核对项目下的 ONU。
13. 作为维护人员，我想编辑项目 ONU 备注，以便记录安装、迁移或现场说明。
14. 作为维护人员，我想从项目列表移除 ONU 时只删除本地关联，以便不会误删 OLT 实机 ONU。
15. 作为维护人员，我想项目详情打开时尽量刷新 ONU 当前状态，以便看到更接近现场的运行情况。
16. 作为维护人员，我想设备离线时仍能看到加入项目时的快照，以便继续进行本地核对。
17. 作为维护人员，我想在 ONU 安装查询的生成方案弹窗中直接选择项目模板，以便不用手动输入项目 VLAN。
18. 作为维护人员，我想项目模板显示项目名称和 VLAN，以便选择模板时能确认不会选错项目。
19. 作为维护人员，我想 ZTE 项目模板复用内部网络命令结构，以便项目 VLAN 能进入一致的配置方案。
20. 作为维护人员，我想 Huawei 项目模板复用内部网络命令结构，以便华为项目 ONU 也能按项目 VLAN 生成方案。
21. 作为维护人员，我想未支持配置方案的设备继续阻止项目模板生成，以便避免误用错误厂商或型号命令。
22. 作为管理员，我想项目模板仍只生成命令预览，以便保持人工确认和只读管理边界。
23. 作为管理员，我想项目和项目 ONU 数据只写入本地 SQLite，以便不会向 OLT 产生任何配置变更。

## 非目标

- 不做 ONU 自动注册。
- 不做业务开通、删除、重启、复位。
- 不做写配置、保存配置、提交配置。
- 不把生成的配置方案自动下发到 OLT。
- 不自动粘贴或执行生成的配置命令。
- 不自动保存配置。
- 不做项目 ONU 自动转移；ONU 已属于其他项目时只提示管理员手工处理。
- 不做项目 Excel 导入导出。
- 不把项目绑定到单台 OLT；同一个项目可以包含分布在不同 OLT 的 ONU。
- 不做公网多用户管理平台。
- 不承诺所有厂商 OID 都已验证。
- Windows 7 x64 桌面版支持内置 Telnet 终端和 ZTE Telnet 只读查询，不依赖系统 Terminal、Expect 或系统 telnet。

## 成功标准

- 本地 `pnpm build` 可通过。
- 后端可以在 `127.0.0.1:8787` 启动。
- 示例数据可以初始化 SQLite。
- ZTE 和 Huawei profile 能在页面中作为只读候选展示。
- 对已验证设备，ONU 查询和详情查询能返回可读结果。
- 对 ZTE 未注册 ONU，页面能从 SNMP 索引解析真实槽、板卡和 PON，并用 PON 台账匹配地址。
- 对未注册 ONU，页面可以按模板生成只读命令预览，并清楚标注不会执行、不下发。
- 复制命令在内嵌浏览器中可用；桌面版内置 Telnet 终端可从首页或配置方案弹窗登录当前 OLT 并按厂商进入配置模式，支持用户手动粘贴剪贴板内容，但不自动粘贴或执行生成命令。
- PON 台账可导出为 Excel；Excel 导入只更新本地台账，不触发设备操作。
- 项目可以新建、编辑、搜索和删除；项目名称全局唯一，项目 VLAN 只能保存 `1-4094` 的单个 VLAN。
- ONU 数据查询可以把 ONU 加入项目，并展示所属项目；同一个 ONU 不能同时属于多个项目。
- 项目详情可以查看项目 ONU 列表、移除项目 ONU 和编辑项目 ONU 备注；移除只删除本地项目关联，不触发设备操作。
- ONU 安装查询可以选择 `项目:项目名称(VLAN号:xxx)` 项目模板，并按 ZTE/Huawei 内部网络结构生成使用项目 VLAN 的命令预览。
- 首页待处理事项只做只读统计和页面跳转，不自动处理 ONU 或写设备。
- Huawei 模板的 `sn-auth` 使用 CLI/SNMP 已验证的原始十六进制 SN；Huawei 自营上网、内部网络和自定义 VLAN 支持 `eth1` 到 `eth4` 端口选择，内部网络固定 VLAN `100`，自定义 VLAN 使用用户输入业务 VLAN，并为所选端口生成 `native-vlan ... priority 0`。
- 敏感运行数据不会进入 git。
- 桌面版运行数据保存在用户数据目录，升级安装包不覆盖 SQLite 台账。
- Windows 7 桌面版只能自动使用包内 SQLite CLI 或用户显式指定的 `OLT_MANAGER_SQLITE_BIN`。现场 SQLite 数据库运行数据不得提交；Win7 发行所需的固定 legacy SQLite CLI `bin/win32/sqlite3.exe` 必须提交并随包发布。

## 风险

- 不同 OLT 软件版本的私有 OID 可能不一致。
- 设备 CLI 输出可能受语言、分页、终端控制字符影响。
- 当前自动化测试覆盖配置方案和部分登录辅助逻辑，但 SNMP/OID 解析样例仍需继续补齐。
- 默认 API 没有认证，不应直接暴露到不可信网络。
- 配置方案依赖现场 VLAN 规划；ZTE 和 Huawei 自定义 VLAN 需要人工输入业务 VLAN，MDU+OTT 需要从同 PON 已配置样板 ONU 或台账读取动态 VLAN。
- 项目 VLAN 由本地项目资料提供；如果项目 VLAN 填写错误，项目模板会生成错误 VLAN 的命令预览，因此保存项目时必须校验 VLAN 范围，并在方案弹窗中明确展示项目名称和 VLAN。
- 项目 ONU 分布在不同 OLT 时，项目详情刷新状态可能遇到部分 OLT 离线，需要保留本地快照并明确提示读取失败。
- Huawei 已注册 ONT SN OID 已在 `0/1/0` 实机样例中验证，更多板卡和 PON 仍需继续积累样例。
- Excel 导入当前以整表替换本地 PON 台账为主，字段级校验和错误报告还需要增强。
- macOS DMG 当前未使用 Apple Developer ID 签名、未经过 Apple 公证；浏览器下载后可能被 Gatekeeper 显示为“已损坏”，只适合可信来源的内部测试分发。

## 待确认问题

- 是否需要对本地 Web 页面增加最小口令。
- Huawei MA5800 已注册 ONT SN OID 在更多板卡和 PON 上是否完全一致。
- PON 台账是否需要更严格的导入模板校验、差异预览和错误报告。
- 后续是否支持多站点数据隔离。
- 配置方案模板是否需要按站点、OLT 或 PON 口设置默认值。
- 正式公开分发 macOS 版本前，何时配置 Apple Developer ID 签名和 Apple 公证。
