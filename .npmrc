# pnpm 11 keeps project-specific settings in pnpm-workspace.yaml.
# Keep this file for registry/auth settings and compatibility with tooling that
# expects a project .npmrc; do not put node-linker or store-dir here.
