# 网管二期登录密码跨平台持久化计划

## 目标

将网管二期登录密码以可迁移的加密密文保存到 SQLite，支持备份还原到 Win7；迁移主密码只由用户输入，永不保存。

## 成功标准

- SQLite 和备份中不出现登录密码明文或迁移主密码。
- 首次登录可用新输入的网管二期密码加密保存；后续可仅输入迁移主密码解密复用。
- 备份还原保留加密密文，迁移主密码不进入备份。
- 错误迁移主密码被拒绝，接口不返回密文或密码。
- 相关测试、构建、语法检查和 diff 检查通过。

## 阶段

1. 现状和边界确认：已完成
2. 加密模块与 SQLite 密文表：已完成
3. 登录 API、界面和备份说明：已完成
4. 回归测试：已完成
5. 最终验证与交接：已完成

## 风险

- 迁移主密码丢失时无法解密旧密文，只能重新输入网管二期登录密码并重新保存。
- 当前不会在代码、测试或日志中写入真实登录密码。
- 现有 OSS/NGB 运行会话仍只存在内存，重启后需要重新登录或输入迁移主密码解锁。
