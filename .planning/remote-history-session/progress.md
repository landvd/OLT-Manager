# 进度记录

## 2026-08-20

- 补充 `remote-access-runtime` 的本机自动登录测试，验证历史光功率按需登录会读取桌面端注入的本机凭据，而不会落入迁移主密码流程。
- 相关测试集现为 39/39 通过，语法检查和 `git diff --check` 通过。

- 用户确认需求：历史光功率查询触发网管二期自动登录，超过 10 分钟自动退出。
- 已读取规划技能规范和既有项目规划文件。
- 已确认当前会话只存内存、没有续期；Mac 当前网管二期状态为未登录。
- 已确认现有 `loginOssNgbSession({ autoLogin: true })` 会读取 Electron `safeStorage` 凭据；未启用本机自动登录且没有迁移主密码时安全失败。
- 新增 `src/remote-history-session.mjs`：按需登录、并发复用、10 分钟租约、401/显式关闭/应用关闭清理，并不接管用户已有手动会话。
- 已将租约接入 `server.mjs` 的历史光功率路径、OSS 资源路由和服务关闭生命周期；未猜测不存在的远端 logout 路径。
- 新增 ADR-074、API/时序文档和变更记录。
- 定向测试 38/38、语法检查和 `git diff --check` 通过。
- 下一步：运行更广回归和 macOS 构建，完成暂存验收。
